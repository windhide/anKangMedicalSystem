spring:
  cloud:
    nacos:
      config:
        server-addr: 47.115.216.208:8848
        prefix: gateway-config
        file-extension: yml
        shared-configs:
          - data-id: gateway-config.yml