spring:
  cloud:
    nacos:
      config:
        server-addr: 8.134.163.127:8848
        prefix: DB-config
        file-extension: yml
        shared-configs:
          - data-id: DB-config.yml
          - data-id: staff-config.yml